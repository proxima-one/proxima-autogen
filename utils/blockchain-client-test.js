var assert = require('assert')

//correct chain

//doesnt fail when it loses

//sets up event handlers for

//transactions

//events

//functions

//blocks

//reconnect



//correct sync

//doesnt fail on closing/errors (switches clients depending on the error code)
describe('blockchain client gen', function() {

  //correct file structure
  it('should have the correct clients', function() {
    assert.equal(true, false);
  });
  //correct client package used
  it('should use the correct packages', function() {
    assert.equal(true, false);
  });
  //correct init and start-up
  it('should be able to be initialized', function() {
    assert.equal(true, false);
  });
});
