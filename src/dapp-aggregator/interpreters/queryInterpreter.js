//given the graphQL file for entities and queries you need to
  // Generate the Typescript client
    // Store the entities within a common file so that they can be referenced by functions and event handlers

  // Create a table list for the DB (...)
  // Generate the golang client
  // Generate  the golang resolvers and models (gql gen)
  // 
